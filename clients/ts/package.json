{
  "name": "@mgfx/demo-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./protocol": "./dist/protocol.js",
    "./ui": "./dist/ui.js"
  },
  "typesVersions": {
    "*": {
      "protocol": ["dist/protocol.d.ts"],
      "ui": ["dist/ui.d.ts"]
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.2"
  }
}
